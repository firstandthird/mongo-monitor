app:
  build: '.'
  dockerfile: 'DockerfileDev'
  volumes:
    - '.:/home/app/src'
  links:
    - 'db:mongo'
  environment:
    FORCE_COLOR: 1
db:
  image: 'mongo'
  command: 'mongod --smallfiles --quiet'
